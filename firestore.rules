
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Access is now public as requested (login removed)
      allow read, write: if true;
    }
  }
}
