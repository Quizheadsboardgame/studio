rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open access for all collections as the app no longer requires login
    // and sensitive client contact details have been removed.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}