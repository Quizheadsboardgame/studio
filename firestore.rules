rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // wide open rules for a blank canvas development experience
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
