{
  "entities": {
    "Site": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Site",
      "type": "object",
      "description": "Represents a physical location or area that requires cleaning services within the CleanOps Hub application.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The common name or identifier for the cleaning site, e.g., 'Anne Mclaren' or 'MRC Epidemiology Level 3'."
        },
        "status": {
          "type": "string",
          "description": "The current operational status of the site, indicating client satisfaction or required actions. Possible values include: 'N/A', 'Client happy', 'Operations request', 'Client concerns', 'Under control', 'Site under action plan', 'Site requires action plan'."
        },
        "notes": {
          "type": "string",
          "description": "Optional notes or comments about the site."
        }
      },
      "required": [
        "name",
        "status"
      ]
    },
    "Cleaner": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Cleaner",
      "type": "object",
      "description": "Represents an individual cleaner managed within the CleanOps Hub application, including their profile, performance rating, and leave tracking.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The full name of the cleaner, e.g., 'Alasdair Strachan'."
        },
        "rating": {
          "type": "string",
          "description": "The current performance rating of the cleaner, based on feedback or operational assessment. Possible values include: 'N/A', 'Excellent feedback', 'Site satisfied', 'Slight improvement needed', 'Needs retraining', 'Under action plan', 'Operational concerns'."
        },
        "notes": {
          "type": "string",
          "description": "Optional notes or comments about the cleaner."
        },
        "holidayAllowance": {
          "type": "number",
          "description": "The total number of holiday days the cleaner is entitled to per year."
        },
        "holidayTaken": {
          "type": "number",
          "description": "The number of holiday days the cleaner has taken so far."
        },
        "sickDaysTaken": {
          "type": "number",
          "description": "The total number of sick days taken by the cleaner."
        }
      },
      "required": [
        "name",
        "rating",
        "holidayAllowance",
        "holidayTaken",
        "sickDaysTaken"
      ]
    },
    "Leave": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Leave",
      "type": "object",
      "description": "Represents a single day of absence (holiday or sickness) for a cleaner.",
      "properties": {
        "cleanerId": {
          "type": "string",
          "description": "The ID of the cleaner taking leave."
        },
        "cleanerName": {
          "type": "string",
          "description": "The name of the cleaner taking leave."
        },
        "type": {
          "type": "string",
          "enum": [
            "holiday",
            "sick"
          ],
          "description": "The type of leave."
        },
        "date": {
          "type": "string",
          "format": "date",
          "description": "The date of the leave (YYYY-MM-DD)."
        },
        "coverAssignments": {
          "type": "array",
          "description": "A list of cover assignments for the leave day.",
          "items": {
            "$ref": "#/backend/entities/CoverAssignment"
          }
        }
      },
      "required": [
        "cleanerId",
        "cleanerName",
        "type",
        "date"
      ]
    },
    "CoverAssignment": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "CoverAssignment",
      "type": "object",
      "description": "Represents the assignment of a cover cleaner to a specific shift.",
      "properties": {
        "site": {
          "type": "string",
          "description": "The site of the shift being covered."
        },
        "coverCleanerName": {
          "type": "string",
          "description": "The name of the cleaner providing cover."
        }
      },
      "required": [
        "site",
        "coverCleanerName"
      ]
    },
    "ActionPlanTask": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ActionPlanTask",
      "type": "object",
      "description": "A single task within an action plan.",
      "properties": {
        "id": {
          "type": "string"
        },
        "description": {
          "type": "string"
        },
        "dueDate": {
          "type": "string",
          "format": "date"
        },
        "completed": {
          "type": "boolean"
        }
      },
      "required": [
        "id",
        "description",
        "dueDate",
        "completed"
      ]
    },
    "ActionPlan": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ActionPlan",
      "type": "object",
      "description": "An action plan for a site or a cleaner.",
      "properties": {
        "targetName": {
          "type": "string"
        },
        "targetType": {
          "type": "string",
          "enum": [
            "site",
            "cleaner"
          ]
        },
        "tasks": {
          "type": "array",
          "items": {
            "$ref": "#/backend/entities/ActionPlanTask"
          }
        },
        "notes": {
          "type": "string"
        }
      },
      "required": [
        "targetName",
        "targetType",
        "tasks"
      ]
    },
    "ScheduleEntry": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ScheduleEntry",
      "type": "object",
      "description": "Represents a single entry in the cleaning schedule.",
      "properties": {
        "site": {
          "type": "string",
          "description": "The name of the site being cleaned."
        },
        "cleaner": {
          "type": "string",
          "description": "The name of the cleaner assigned."
        },
        "start": {
          "type": "string",
          "description": "The start time of the shift."
        },
        "finish": {
          "type": "string",
          "description": "The finish time of the shift."
        }
      },
      "required": [
        "site",
        "cleaner",
        "start",
        "finish"
      ]
    },
    "Consumable": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Consumable",
      "type": "object",
      "description": "A consumable supply item for a site, including its name and ordering code.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the supply item."
        },
        "orderingCode": {
          "type": "string",
          "description": "The ordering code for the item."
        }
      },
      "required": [
        "name",
        "orderingCode"
      ]
    },
    "MonthlySupplyOrder": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "MonthlySupplyOrder",
      "type": "object",
      "description": "Represents the quantity of a consumable ordered for a specific site in a given month.",
      "properties": {
        "siteId": {
          "type": "string"
        },
        "consumableId": {
          "type": "string"
        },
        "month": {
          "type": "number",
          "description": "The month of the order (1-12)."
        },
        "year": {
          "type": "number",
          "description": "The year of the order."
        },
        "quantity": {
          "type": "number",
          "description": "The quantity ordered for the month."
        }
      },
      "required": [
        "siteId",
        "consumableId",
        "month",
        "year",
        "quantity"
      ]
    },
     "MonthlyAudit": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Monthly Audit",
      "type": "object",
      "description": "Represents a site audit for a specific month.",
      "properties": {
        "siteId": {
          "type": "string"
        },
        "month": {
          "type": "number"
        },
        "year": {
          "type": "number"
        },
        "status": {
          "type": "string",
          "enum": [
            "Not Booked",
            "Emailed Client",
            "Booked",
            "Completed"
          ],
          "description": "The current status of the audit for the site."
        },
        "score": {
          "type": "number"
        },
        "bookedDate": {
          "type": "string",
          "format": "date",
          "description": "The date the audit is booked for or was completed on."
        },
        "bookedTime": {
          "type": "string",
          "description": "The time the audit is booked for."
        }
      },
      "required": [
        "siteId",
        "month",
        "year"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/sites/{siteId}",
        "definition": {
          "entityName": "Site",
          "schema": {
            "$ref": "#/backend/entities/Site"
          },
          "description": "Top-level collection storing details about each physical cleaning site."
        }
      },
      {
        "path": "/cleaners/{cleanerId}",
        "definition": {
          "entityName": "Cleaner",
          "schema": {
            "$ref": "#/backend/entities/Cleaner"
          },
          "description": "Top-level collection storing profiles and performance ratings for each cleaner."
        }
      },
      {
        "path": "/actionPlans/{actionPlanId}",
        "definition": {
          "entityName": "ActionPlan",
          "schema": {
            "$ref": "#/backend/entities/ActionPlan"
          },
          "description": "Collection storing action plans for sites and cleaners."
        }
      },
      {
        "path": "/leave/{leaveId}",
        "definition": {
          "entityName": "Leave",
          "schema": {
            "$ref": "#/backend/entities/Leave"
          },
          "description": "Collection storing all leave records (holiday and sickness) for cleaners."
        }
      },
      {
        "path": "/schedule/{scheduleEntryId}",
        "definition": {
          "entityName": "ScheduleEntry",
          "schema": {
            "$ref": "#/backend/entities/ScheduleEntry"
          },
          "description": "Top-level collection storing the cleaning schedule."
        }
      },
      {
        "path": "/sites/{siteId}/consumables/{consumableId}",
        "definition": {
          "entityName": "Consumable",
          "schema": {
            "$ref": "#/backend/entities/Consumable"
          },
          "description": "Sub-collection storing consumable items specific to a site."
        }
      },
      {
        "path": "/supplyOrders/{orderId}",
        "definition": {
          "entityName": "MonthlySupplyOrder",
          "schema": {
            "$ref": "#/backend/entities/MonthlySupplyOrder"
          },
          "description": "Collection storing monthly supply order records for all sites. The document ID is a composite key: {siteId}-{consumableId}-{yyyy}-{MM}."
        }
      },
      {
        "path": "/monthlyAudits/{auditId}",
        "definition": {
          "entityName": "MonthlyAudit",
          "schema": {
            "$ref": "#/backend/entities/MonthlyAudit"
          },
          "description": "Collection storing monthly audit records for all sites. The document ID is a composite key: {siteId}-{yyyy}-{MM}."
        }
      }
    ]
  }
}
